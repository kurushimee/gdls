# Helper for 02_autoload_reference.gd: registered as autoload `GameState` in project.godot.
# Both gdls (via the class_name registry) and Godot (via project autoload) must resolve
# `GameState` to this script and `GameState.score` to its typed `int` member.
extends Node

var score: int = 0
